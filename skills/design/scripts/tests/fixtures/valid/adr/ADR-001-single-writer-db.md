# ADR-001: Single-writer database

Status: accepted

This file lives under `adr/`, whose format STO-100 owns. The design validator
skips the whole `adr/` subtree — it must not try to parse this as a CMP-/IF-
artifact. Its presence in the valid fixture proves the skip.
